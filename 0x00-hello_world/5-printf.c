#include <stdio.h>
/**
 * main - function
 * used to print the character, string, float, integer,
 * octal and hexadecimal value onto te screen.
 * Use %(option) as a specifier.
 * Return: 0
*/
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
